# Frontend - AI HACK APP1

Next.js App Router / Feature-Sliced Design / shadcn/ui（Base UI 版）のフロントエンドです。起動手順とデモの通し方はリポジトリ直下の [README.md](../README.md) を見てください。

```bash
pnpm install
pnpm run dev
pnpm run verify
pnpm run build
pnpm exec playwright install --with-deps chromium
pnpm run test:e2e
```
